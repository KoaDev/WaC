<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:Configurator.ViewModels"
             xmlns:converters="clr-namespace:Configurator.Converters"
             x:Class="Configurator.Views.ResourceListView"
             x:Name="this">
    <ContentView.Resources>
        <converters:IsNotNullConverter x:Key="IsNotNullConverter" />
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="CornerRadius" Value="0" />
            <Setter Property="FontAttributes" Value="Bold" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="TextColor" Value="White" />
            <Setter Property="HeightRequest" Value="32" />
            <Setter Property="VisualStateManager.VisualStateGroups">
                <VisualStateGroupList>
                    <VisualStateGroup x:Name="CommonStates">
                        <VisualState x:Name="Normal">
                            <VisualState.Setters>
                                <Setter Property="BackgroundColor" Value="#007ACC" />
                                <Setter Property="Opacity" Value="1" />
                            </VisualState.Setters>
                        </VisualState>
                        <VisualState x:Name="Disabled">
                            <VisualState.Setters>
                                <Setter Property="BackgroundColor" Value="#3A3A3A" />
                                <Setter Property="TextColor" Value="#9A9A9A" />
                                <Setter Property="Opacity" Value="0.6" />
                            </VisualState.Setters>
                        </VisualState>
                    </VisualStateGroup>
                </VisualStateGroupList>
            </Setter>
        </Style>
        <Style x:Key="SuccessButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="VisualStateManager.VisualStateGroups">
                <VisualStateGroupList>
                    <VisualStateGroup x:Name="CommonStates">
                        <VisualState x:Name="Normal">
                            <VisualState.Setters>
                                <Setter Property="BackgroundColor" Value="#16825D" />
                                <Setter Property="TextColor" Value="White" />
                                <Setter Property="Opacity" Value="1" />
                            </VisualState.Setters>
                        </VisualState>
                        <VisualState x:Name="Disabled">
                            <VisualState.Setters>
                                <Setter Property="BackgroundColor" Value="#3A3A3A" />
                                <Setter Property="TextColor" Value="#9A9A9A" />
                                <Setter Property="Opacity" Value="0.6" />
                            </VisualState.Setters>
                        </VisualState>
                    </VisualStateGroup>
                </VisualStateGroupList>
            </Setter>
        </Style>
    </ContentView.Resources>
    <Border BackgroundColor="#2D2D30"
            Stroke="#3E3E42"
            StrokeThickness="1"
            Padding="8"
            Margin="0,8,0,0"
            MinimumHeightRequest="100">
        <CollectionView ItemsSource="{Binding Source={x:Reference this}, Path=ItemsSource}">
            <CollectionView.EmptyView>
                <Label Text="Open a configuration file to see the resources." 
                       HorizontalOptions="Center" 
                       VerticalOptions="Center" 
                       TextColor="#9E9E9E"/>
            </CollectionView.EmptyView>
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Border BackgroundColor="#383838"
                            Stroke="#484848"
                            StrokeThickness="1"
                            Padding="8"
                            Margin="0,0,0,4">
                        <Grid ColumnDefinitions="*,Auto">
                            <VerticalStackLayout Grid.Column="0" Spacing="2">
                                <Label Text="{Binding Name}"
                                       FontAttributes="Bold"
                                       FontSize="13"
                                       TextColor="#FFFFFF"/>
                                <Label Text="{Binding Type}"
                                       FontSize="10"
                                       TextColor="#D4D4D4"
                                       FontAttributes="Italic"/>
                                <HorizontalStackLayout Spacing="5">
                                    <Label Text="Status:"
                                           TextColor="#9E9E9E"
                                           FontSize="10"/>
                                    <Label Text="{Binding Status}"
                                           TextColor="{Binding StatusColor}"
                                           FontAttributes="Bold"
                                           FontSize="10"/>
                                </HorizontalStackLayout>
                            </VerticalStackLayout>
                            <HorizontalStackLayout Grid.Column="1" Spacing="8" VerticalOptions="Center">
                                <Button Text="Test" Command="{Binding TestCommand}" HeightRequest="8" Style="{StaticResource PrimaryButtonStyle}" />
                                <Button Text="Apply" Command="{Binding ApplyCommand}" HeightRequest="8" Style="{StaticResource SuccessButtonStyle}" />
                            </HorizontalStackLayout>
                        </Grid>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Border>
</ContentView>
